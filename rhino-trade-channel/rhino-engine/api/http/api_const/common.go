package api_const

const (
	ApiVersionPrefix = "/api/v1"
)

const (
	RoutePing      = ApiVersionPrefix + "/ping"
	RootRouteOrder = ApiVersionPrefix + "/order"

	ParamId   = "id"
	ParamDate = "date"
)
